# Anti-Slop compatibility entry point

The maintained Anti-Slop v2 router is [anti-slop/INDEX.md](anti-slop/INDEX.md).

Load that index first. It contains the compact always-on truth gate and routes to only the modules relevant to the current direction. Do not load every Anti-Slop module by default.
